# החלטות עבודה

## גיבוי אוטומטי ל-Git (2026-05-26)

**ההחלטה:** כל פעם שדין מבקש "תגבה" / "גיבוי" / "backup" — לבצע אוטומטית:
1. `git add .`
2. `git commit -m "..."` עם הודעה מתארת
3. `git push`

**בלי לשאול אישור מראש. בלי לבקש סיסמאות. בדיוק כפי שעשינו בפעם הראשונה.**

**Remote:** https://github.com/deanshvika/dean_office

**הגנה על סודות:**
- ה-`.gitignore` הקיים מסנן: `.wwebjs_auth/`, `.env`, `node_modules/`, `base44_token.json`, `*password*`, `*credentials*`, `base44_userdata/`
- לפני כל push, לוודא שאין סודות חדשים ב-staging (בדיקה מהירה: `git ls-files --cached | grep -iE "(\.env$|token|password|credential|\.wwebjs_auth)"`)
- אם מתגלה קובץ סוד חדש שלא ב-.gitignore — לעצור, להוסיף ל-.gitignore, ורק אז להמשיך

**הקבצים שנשארים תמיד מקומיים (אסור לדחוף):**
- `.wwebjs_auth/` — סשני WhatsApp
- `whatsapp-tool/.env` — API keys
- `whatsapp-tool/base44_token.json` — טוקני Base44
- `node_modules/` — חבילות npm
- כל קובץ עם `password` / `credentials` בשם

## Alias "תעלה לענן" (2026-05-26)

**ההחלטה:** הוספת alias קצר שעושה יותר מ"תגבה" — הוא גם שומר את הקשר השיחה לקבצי MD ולזיכרון, ואז דוחף.

**ההפעלה:** כשדין אומר "תעלה לענן", Claude מבצע ללא אישור:
1. אם בשיחה הנוכחית התקבלו החלטות חדשות → להוסיף ל-`desicion.md`
2. אם נוצרו כללי עבודה / instructions חדשים → להוסיף ל-`CLAUDE.md`
3. אם התגלו עובדות לזכור בעתיד → לשמור ב-memory
4. `git add . && git commit && git push`

**ההבדל מ"תגבה":** "תגבה" רק דוחף את המצב הקיים. "תעלה לענן" קודם **מתעד** את מה שלמדנו בשיחה ואז דוחף — כך השיחה הבאה ממשיכה ברצף מלא.

## מערכת מדידת מאמנים — אמות מידה וציונים (2026-06-09)

**ההחלטה:** להקים מסגרת רשמית למדידה וציון של 23 המאמנים בתוכנית הספורט.

**מבנה המסגרת (מתועד ב-`אמות_מידה_מאמנים.md`):**
- 5 תחומי מדידה עם משקלים: מקצועיות אימון 30%, נוכחות ואמינות 25%, שביעות רצון לקוחות 20%, התנהלות מול ההנהלה 15%, תרומה וצמיחה 10%
- כל מדד בסולם 1–5; ציון סופי משוקלל 0–100 עם 5 דרגות סיווג (מצטיין / טוב מאוד / תקין / דורש שיפור / בעייתי)
- לכל מדד הוגדרו: מקור נתון + תדירות מדידה (שוטף / חודשי / רבעוני / עונתי / שנתי)

**גיליון הציונים (Google Sheets):**
- URL שמור ב-`coach_eval_url.txt`
- נבנה אוטומטית עם puppeteer/clipboard-paste; עמודת "ציון סופי" מכילה נוסחה שמחשבת לבד את הציון המשוקלל כשמזינים 1–5
- סקריפטים: `make_coach_eval_sheet.js` (יצירה מלאה) + `gen_coach_tsv.js` (יצירת TSV לclipboard)

**סטטוס פתוח:** דין ביקש לכוונן את המדדים בכל תחום לפי מה שכואב לו בפועל בשטח — השיחה נעצרה לפני שסיים לענות על שאלות החידוד. **להמשיך משם בפעם הבאה.**

## הגדרת תפקיד לאריק מונטיבילסקי — רכז פעילות (2026-07-01)

**ההקשר:** דין בונה הגדרת תפקיד לאריק מונטיבילסקי בתור **רכז פעילות** — אחראי על ניהול מערך המאמנים בשטח.

**ההחלטות שסוכמו בשיחה (בלב ההגדרה):**
1. **סמכות:** אריק **מחליט ופועל עצמאית** בתחומי אחריותו. פונה לדין לעדכון/התייעצות **רק במקרה הצורך** (השלכה תקציבית חריגה, סיום עבודה קבוע, קונפליקט מול מוקד) — לא לאישור שוטף.
2. **תיעוד:** אריק **אחראי על התיעוד והדיווח** (נוכחות, החלפות, הערכות).
3. **ביקורת שטח:** **לפחות 2 צפיות מוקד בשבוע**.

**תחומי אחריות:** ניהול שוטף של המאמנים · נוכחות ודייקנות · ציוד וסדר · החלפות וגיבוי · ביקורת והערכה בשטח.

**הקבצים שנוצרו (על הדסקטופ, בתיקיית הפרויקט):**
- `הגדרת_תפקיד_אריק_רכז_פעילות.md` + `.docx` — **הטיוטה המלאה** (כולל מדדי הצלחה וטופס הערכת מאמן בנספח א').
- `הגדרת_תפקיד_אריק_קצר.docx` — **גרסה קצרה ורשמית** (פסקה מנוסחת למכתב מינוי/חוזה).
- שני המסמכים הועלו ל-Google Drive ונפתחו כמסמכי גוגל.

**נקודות פתוחות שדין ישלים בעתיד:**
- תקרת תקציב מספרית ל"השלכה תקציבית חריגה" (מעל ₪X דורש אישור).
- לחבר "בנק מחליפים" לפי אזור לרשימת המאמנים הפעילים.

## סקיל text-to-gdocs — העברת טקסט ל-Google Docs (2026-07-01)

**ההחלטה:** נשמר סקיל קבוע `.claude/skills/text-to-gdocs/` שהופך טקסט/HTML למסמך Google Docs מעוצב (עברית RTL). מופעל ב"תעביר לדוקס" / "תעשה מזה מסמך גוגל".

**השיטה שעובדת:** HTML → DOCX (Word COM) → העלאה ל-Drive (חלון כרום חדש, קליקים, **הדבקה עיוורת**, Enter — הכל בסקריפט אחד) → קליק על הודעת ההשלמה לפתיחה כמסמך גוגל. **הדבקה ישירה לדוקס פתוח לא עובדת.** פירוט מלא ב-SKILL.md ובזיכרון [[יצירת-מסמך-גוגל-דוקס-מעוצב-השיטה-שעובדת]].

## מסמך הנחיות למורות — יום שיא (יוני 2026, שוחזר 2026-07-01)

**המצב:** מסמך "הנחיות למורות — יום שיא הורים וילדים" (ניקה) **הושלם והועלה ל-Google Docs**.

**קישור חי:** https://docs.google.com/document/d/1TdVRjp-58kMLazVmxwRQvXJ3EmAyLd31Yc--DXGGvVM/edit (שמור גם ב-`doc_morot_url.txt`).

**קבצים (tracked בגיט):** `מסמך_הנחיות_למורות_יום_שיא.md` (מקור לעריכה) · `doc_morot.html`/`.txt` (להדבקה) · `make_doc_morot.js` (סקריפט הבנייה) · `doc_morot_check.png` (אימות).

**להמשך מסשן חדש:** לשינוי תוכן — לערוך את ה-MD, לסנכרן ל-HTML/TXT, ולהריץ `node make_doc_morot.js`. פירוט מלא בזיכרון [[project-doc-morot-yom-shia]].

## מנוע תוכן לימודי NIKA + לוח בקרה (2026-07-01)

**ההקשר:** המנהל חי סיני התחיל לכתוב תכניות לימוד (חשבון/שפה/אנגלית). כשביקשו מ-Gemini לייצר עוד תכניות "על אותם עקרונות" — חזרו טעויות עקביות ב**מספרים** (בחשבון) וב**ניקוד עברי**. חי סיני גיבש תהליך 7-שלבים (בתיקיית `Downloads/חי סיני אורינות`). המטרה: מערכת שמייצרת תוכן חדש שוטף בלי טעויות, לשכפול למרכזים נוספים.

**ההחלטה הארכיטקטונית (הלב):** להוציא את המספרים ואת הניקוד **מהידיים של ה-AI** ולהעביר לקוד דטרמיניסטי + בנק מנוקד סגור. 3 שכבות: **נתונים (Day Spec YAML) → ולידציה (שער חובה) → תבנית+נכסים → רינדור PDF**. הקוד מדביק אייקון בדיוק `count` פעמים (אפס טעות מספרית); טקסט מנוקד נשלף מבנק סגור (אפס טעות ניקוד).

**החלטות שסוכמו עם דין:**
1. **ויזואלים:** ספריית נכסים קבועה + רינדור בקוד (לא AI שמצייר דף שלם — זה מקור הטעויות).
2. **ניקוד:** בנק מנוקד סגור + Dicta Nakdan למילים חדשות. **מדיניות (מהדוגמאות):** חשבון = ללא ניקוד; שפה שכבה א' = ניקוד מלא; אנגלית שכבה א' = עברית מנוקדת; ב'–ג' = ללא ניקוד.
3. **מותג:** שפת "ניקה ספורט וקהילה" — רקע לבן, גרדיאנטים ניאוניים (מגנטה/כחול-סגול/ליים), אייקוני line-art בגרדיאנט (בלי מילוי), פינות מעוגלות. תועד ב-`system/brand_rules.md`.
4. **אפליקציה:** דין ביקש **לוח בקרה מקומי בדפדפן** (לא רק מנוע CLI) — טופס → תצוגה חיה → ולידציה → הורדת PDF.

**מה נבנה (`nika-content-engine/`):** מנוע Node ESM מלא (js-yaml + playwright + pdf-lib) + **לוח בקרה** (`scripts/server.mjs` + `app/`). פיילוט `grade_1_math_day_1` (4 עמודים תואמי-זהב) אומת: דטרמיניזם מספרי, שער ולידציה חוסם טעויות, ניקוד לפי מדיניות.

**הפעלת הלוח:** לחיצה כפולה על קיצור **"לוח בקרה NIKA"** בשולחן העבודה (→ `start-app.bat`), או `cd nika-content-engine && node scripts/server.mjs` → `http://localhost:5178`. **אל תסגור את חלון השרת השחור בזמן עבודה.**

**נקודות פתוחות:** (1) נכסי מותג רשמיים ל-`assets/` (לוגו/פונטים/איורים — כרגע סט אייקוני סטארטר). (2) ימי שפה/אנגלית לבחינת הניקוד. (3) שכבות ב'–ג'. פירוט מלא בזיכרון [[project-nika-content-engine]] ובתכנית `~/.claude/plans/elegant-dreaming-seahorse.md`.

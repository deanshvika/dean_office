# הוראות עבודה — Dean Office

## גיבוי = git push אוטומטי

כשדין אומר **"תגבה"**, **"גיבוי"**, **"backup"** או כל וריאציה דומה — בצע מיד את הרצף הבא ללא בקשת אישור:

```
git add .
git commit -m "<תיאור קצר של מה השתנה>"
git push
```

**אל תשאל "בטוח?"**. **אל תבקש אישור**. **אל תבקש סיסמאות**. פשוט תבצע.

## "תעלה לענן" = שמירת הקשר מלאה + push

כשדין אומר **"תעלה לענן"** — זו פעולה רחבה יותר מ"תגבה". בצע את הרצף הבא ללא בקשת אישור:

1. **תיעוד הקשר השיחה:**
   - אם נקבעו החלטות חדשות בשיחה → הוסף ל-`desicion.md`
   - אם נוצרו כללי עבודה / הוראות חדשות → הוסף ל-`CLAUDE.md`
   - אם התגלו עובדות שכדאי לזכור בשיחות עתידיות → שמור ב-memory (`C:\Users\דין\.claude\projects\c--Users-----Desktop-ANTIGRAVITY----------\memory\`)
2. **גיבוי לגיט:** `git add . && git commit && git push`

**ההבדל בין "תגבה" ל"תעלה לענן":**
- "תגבה" = רק push של המצב הנוכחי של הקבצים
- "תעלה לענן" = קודם לתעד את ההקשר ואת ההחלטות בקבצי MD ובזיכרון, ואז push

**אל תשאל אישור. בלי סיסמאות. פשוט תבצע.**

**Remote קיים:** `https://github.com/deanshvika/dean_office`

## בדיקת סודות לפני push

לפני כל push, בצע בדיקה זריזה שלא נכנסו סודות חדשים:
```
git ls-files --cached | grep -iE "(\.env$|token\.json|password|credential|\.wwebjs_auth)"
```
אם הפלט ריק — דחוף. אם משהו צץ — עצור, הוסף ל-.gitignore, הסר מה-cache (`git rm --cached`), ואז דחוף.

## קבצים אסורים לדחיפה (כבר ב-.gitignore)

- `.wwebjs_auth/` — סשני WhatsApp
- `.env` — API keys
- `node_modules/` — חבילות
- `base44_token.json` — טוקני Base44
- `base44_userdata/` — דאטה של דפדפן Base44
- `*password*`, `*credentials*`

הקבצים האלה **חייבים להישאר** במחשב המקומי — הבוט והכלים תלויים בהם.

## העברת טקסט ל-Google Docs = סקיל text-to-gdocs

כשדין אומר **"תעביר לדוקס"**, **"תעשה מזה מסמך גוגל"**, **"תפתח את זה בדוקס"** או מבקש להפוך תוכן למסמך Google Docs — השתמש בסקיל **`text-to-gdocs`** (`.claude/skills/text-to-gdocs/`).

**השיטה שעובדת (אל תנסה אחרת):** HTML (RTL) → DOCX דרך Word COM → העלאה ל-Google Drive → פתיחה כמסמך גוגל.
**מה שלא עובד:** הדבקת HTML ישירות לתוך מסמך Docs פתוח (Ctrl+V) — גוגל דוקס לא קולט. אל תבזבז על זה זמן.
**קריטי:** VS Code הוא Electron (אותה מחלקת חלון כמו Chrome) וחוטף פוקוס → כל רצף ההעלאה חייב לרוץ בסקריפט PowerShell אחד, עם הדבקה "עיוורת" (בלי FindWindow). תמיד לאמת עם צילום מסך.

## יצירת Google Sheet = XLSX מקומי → העלאה ל-Drive

**Sheets API חסום.** גוגל חסמה את ה-OAuth client של הפרויקט (`1072944905499-…`) — כל קריאה מחזירה *"האפליקציה הזו חסומה"*. זה משבית את `outlook_candidates_to_sheet.js`, `candidates_tracking_sheet.js` ו-`make_school_coach_sheet.js`. **אל תנסה את המסלול הזה.** (לתיקון עתידי: לאמת/לפרסם את האפליקציה ב-Google Cloud Console.)
**Apps Script גם לא פתרון** — ההרצה הראשונה קופצת "דרושה הרשאה" שדורש קליק אנושי.

**השיטה שעובדת** (אותו עיקרון כמו סקיל `text-to-gdocs`, רק לגיליונות):
1. לבנות XLSX מקומית עם `exceljs` — נותן RTL, הקפאת כותרת, צבעים, dropdown (`dataValidation`), צביעה מותנית ונוסחאות. **הכל שורד את ההמרה של גוגל.** (`xlsx`/SheetJS community לא מספיק — סגנונות ו-validation הם פיצ'רי Pro.)
2. `node upload_to_drive.js "<path>" --open`
3. בגיליון: **קובץ → "שמירה בתור Google Sheets"** (זה שם הפריט המדויק) — ממיר ממצב תאימות XLSX לגיליון גוגל אמיתי.

**מגבלות Excel שחוזרות ונשכחות:** שם לשונית לא יכול להכיל `/ ? * : [ ]`. נוסחאות חייבות לעבוד בשתי הסביבות — `COUNTIF`+`FILTER` כן, `IF` מעל טווח לא (דורש `ARRAYFORMULA` בגוגל שנשבר ב-Excel).

**`upload_to_drive.js` מחליף את שלב ה-upload של הסקיל.** שלב ה-upload בסקיל משתמש בהדבקה עיוורת ו-SendKeys, ו**נכשל בפועל** כש-VS Code חוטף פוקוס (הוא Electron). `upload_to_drive.js` עושה הכל דרך CDP: `page.mouse` בתוך הדף + `waitForFileChooser` — הדפדפן מיירט את דיאלוג הקובץ, אפס תלות בפוקוס, אפס קליקים עיוורים.

## אוטומציית דפדפן (Sheets/Docs/Drive) = פרופיל Chrome ייעודי בפורט 9222

כשצריך לעבוד מול Google Sheets / Docs / Drive או כל אתר שדורש התחברות — **התחבר לפרופיל האוטומציה הייעודי של דין**, לא לפתוח דפדפן חדש/ריק ולא Playwright עם פרופיל זמני.

**למה פרופיל נפרד:** Chrome 149 חוסם remote-debugging על הפרופיל הרגיל הרץ (מגבלת אבטחה מ-v136+). פרופיל זמני + העתקת cookies נכשל (App-Bound Encryption). לכן יש פרופיל ייעודי, מחובר פעם אחת לחשבון `deanshvika@gmail.com`.

- **user-data-dir:** `C:\Users\דין\ChromeAutomation` | **פורט קבוע:** `9222`
- **קיצור הפעלה:** `Chrome אוטומציה (9222).lnk` על שולחן העבודה
- **כלי עזר מוכן:** `chrome_automation.js` (`getBrowser()` מפעיל את הפרופיל אם צריך ומתחבר; `openPage(b,url)`). הרצה ישירה `node chrome_automation.js` = בדיקת חיבור + זיהוי החשבון.
- **לנהוג בדף דרך API בלבד:** `page.mouse` / `page.keyboard` / `navigator.clipboard.writeText` + Ctrl+V. הקלט הולך לדף הספציפי — בטוח.
- **אסור בהחלט:** הקלקות/SendKeys עיוורות ברמת מערכת ההפעלה. כל חלונות ה-Chrome חופפים באותן קואורדינטות, וקליק עיוור עלול לנחות על אפליקציית Base44 (שיבוצי ניקה) ולבצע חילוף/שינוי שכר אמיתי בייצור.
- אם `node chrome_automation.js` מדווח "לא מזוהה" — צריך התחברות חד-פעמית ידנית של דין בחלון הפרופיל, ואז זה נשמר לתמיד.

## מנוע התוכן הלימודי — `nika-content-engine/`

מנוע שמייצר דפי עבודה (חשבון/שפה/אנגלית, שכבות א' + ב'–ג') ל-NIKA **ללא טעויות מספרים/ניקוד**. נבנה לפי 7 מסמכי ההנחיה של חי סיני (תיקיית `Downloads/חי סיני אורינות`).

- **עיקרון:** נתונים (Day Spec YAML) → ולידציה (שער חובה) → תבנית+נכסים → רינדור PDF/PNG. הקוד מדביק אייקון בדיוק `count` פעמים (אפס טעות מספרית); טקסט מנוקד נשלף מבנק סגור (אפס טעות ניקוד).
- **לוח בקרה (אפליקציה):** קיצור **"לוח בקרה NIKA"** בשולחן העבודה (→ `start-app.bat`) או `node scripts/server.mjs` → `http://localhost:5178`. טופס → תצוגה חיה → ולידציה → הורדת PDF. **אסור לסגור את חלון השרת בזמן עבודה.** קוד: `scripts/server.mjs` + `app/` — מעטפת דקה שמשתמשת חוזר ב-`validateDay`/`renderPage`.
- **הרצת יום (CLI):** `cd nika-content-engine && node scripts/run_batch.mjs --day <day_id>`
- **ולידציה בלבד:** `node scripts/validate_page.mjs <day_id>`
- **מחסנית:** Node ESM, `js-yaml` (מקומי), `playwright`+`pdf-lib` (מ-node_modules של השורש). אימות ויזואלי תמיד עם צילום מסך (Playwright).
- **פיילוט מאומת:** `grade_1_math_day_1` (4 עמודים, תואם לדף הזהב). ניקוד: חשבון=ללא, שפה/אנגלית שכבה א'=מנוקד.
- **נכסים:** `assets/icons/` = סט סטארטר line-art. להחליף בלוגו/פונטים/איורים רשמיים (`assets/README.md`).
- **יום חדש = קובץ Day Spec קטן** (לא נגיעה בקוד). מסמכי DNA ב-`system/`.

## מפת המוקדים — `map_sites/`

מפה אינטראקטיבית של כל מוקדי ניקה לפי מיקום גאוגרפי, צבועה לפי ודאות שיבוץ המאמן.

- **קישור לשיתוף:** `https://claude.ai/code/artifact/c042c619-a373-4e3b-a7c6-1725bc05b05e` — פרטי כברירת מחדל, משתפים מתפריט השיתוף בעמוד. כרגע מיועד להנהלה בלבד (מכיל שמות מאמנים, פערי שיבוץ והערות פנימיות).
- **כשדין אומר "תרענן את המפה" / "תעדכן את המפה":** `node map_sites/refresh.js` ואז **פרסום חוזר של אותו נתיב קובץ** דרך `Artifact` — זה שומר על אותו קישור. אל תיצור ארטיפקט חדש.
- הרענון קורא את הגיליונות החיים דרך פרופיל האוטומציה, ומסיים בדיף של מה שהשתנה בשיבוצים.
- **הארטיפקט לא מסוגל למשוך נתונים חיים בעצמו.** ההרשאות הזמינות הן `downloads` ו-`mcp` בלבד. אין מסלול "מפה שמתעדכנת לבד" — אל תבזבז על זה זמן.
- פרטים מלאים, מקורות המיקום והמלכודות: `map_sites/README.md`.

## הודעות commit

- בעברית או באנגלית, מה שמתאים יותר לשינוי
- קצר ומתאר את ה-WHY, לא רק WHAT
- אם הגיבוי ספונטני בלי שינוי מוגדר, פשוט: `"Backup <תאריך>"`
